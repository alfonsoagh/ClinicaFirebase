<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base (claro) -->
    <style name="Theme.ClinicaSX" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colores de marca -->
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorOnPrimary">@color/md_onPrimary</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorOnSecondary">@color/md_onSecondary</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@color/md_onSurface</item>
        <item name="colorSurfaceVariant">@color/md_surfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/md_onSurfaceVariant</item>
        <item name="colorOutline">@color/md_outline</item>

        <!-- Status/Navigation -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@android:color/black</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <!-- Componentes Material por defecto -->
        <item name="materialButtonStyle">@style/Widget.Material3.Button</item>
        <item name="materialButtonOutlinedStyle">@style/Widget.Material3.Button.OutlinedButton</item>

        <!-- Redondeo global (botones, campos) -->
        <item name="shapeAppearanceSmallComponent">@style/Shape.Rounded.Medium</item>
        <item name="shapeAppearanceMediumComponent">@style/Shape.Rounded.Large</item>
        <item name="shapeAppearanceLargeComponent">@style/Shape.Rounded.Large</item>
    </style>

    <!-- Variante explícita sin ActionBar (si alguna Activity la requiere) -->
    <style name="Theme.ClinicaSX.NoActionBar" parent="Theme.Material3.Light.NoActionBar" />

    <!-- Overlays para Toolbar / Popups -->
    <style name="Theme.ClinicaSX.AppBarOverlay" parent="ThemeOverlay.Material3.ActionBar" />
    <style name="Theme.ClinicaSX.PopupOverlay" parent="ThemeOverlay.Material3" />

    <!-- Shapes base y variantes -->
    <style name="Shape.Rounded">
        <item name="cornerFamily">rounded</item>
    </style>
    <style name="Shape.Rounded.Medium">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">14dp</item>
    </style>
    <style name="Shape.Rounded.Large">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>
    <!-- Estilo base Shape para referencias genéricas -->
    <style name="Shape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

</resources>
